---
import ServicePage from '@/components/ServicePage.astro';
import { SERVICES } from '@/data/services';
export function getStaticPaths() { return SERVICES.map((s) => ({ params: { service: s.slug }, props: { s } })); }
---
<ServicePage lang="bg" s={Astro.props.s} />
